# Copy to .env and replace YOUR_DB_PASSWORD with your Atlas database user password.
# If the password contains special characters (@, #, :, /, etc.), URL-encode them first.
MONGO_URI=mongodb+srv://jamesvillaluz30_db_user:YOUR_DB_PASSWORD@cluster0.oowdzd4.mongodb.net/webprog?retryWrites=true&w=majority&appName=Cluster0

PORT=5001
JWT_SECRET=your_jwt_secret_change_in_production
NODE_ENV=production
